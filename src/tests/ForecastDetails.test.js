import { shallowMount } from '@vue/test-utils';
import ForecastDetails from '../components/ForecastDetails.vue';

describe('ForecastDetails', () => {
  it('should render', () => {
    // const wrapper = shallowMount(ForecastDetails);

    expect(2).toEqual(2);
  });
});
